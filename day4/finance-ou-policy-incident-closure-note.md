Resolved. Cause: Floor 3 DHCP scope DNS Option 006 remained stale after migration and assigned decommissioned DNS servers, causing domain controller name-resolution failure and Group Policy processing failure on 3 of 4 Finance endpoints. Action: Updated DHCP Scope Option 006 to current DNS (10.10.0.10), removed decommissioned DNS entries, renewed affected client leases, refreshed DNS cache, and re-ran Group Policy processing. Preventive: Enforce mandatory pre/post-cutover validation of DHCP Option 006 and block DNS decommission sign-off until all DHCP scopes and reservations are verified clean. User confirmed working.